# Um funcionário recebe um salário fixo mais 4% de comissão sobre as suas vendas.
# Faça um algoritmo que receba o valor do salário fixo do funcionário,
# o valor das suas vendas e que calcule e mostre o salário final do funcionário.


def linhas():
    print(f"=" * 30)

def loja(sal, vendas):
    comis = vendas*0.04
    linhas()
    print(f"O salário fixo do funcionário é de {sal}")
    linhas()
    print(f"A comissão sobre as vendas é de {comis}")
    linhas()
    print(f"O salário final é de {sal+comis}")
    linhas()


sal = float(input("Digite o salário do funcionário: "))
vendas = float(input("Digite o valor total de vendas desse funcionário: "))
loja(sal, vendas)
